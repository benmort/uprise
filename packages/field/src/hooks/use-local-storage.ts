// Moved to @uprise/ui so the volunteer auth flow and the field app share one copy.
// Re-exported here so existing field call sites (e.g. walk-view) stay unchanged.
export { useLocalStorage } from "@uprise/ui";
