// Retiring prog/ui → @uprise/ui. Thin re-export of the shared Radix Popover compound
// (same Popover/PopoverTrigger/PopoverContent API). Repointed to `@uprise/ui` next, then
// deleted. Zero functional regression.
export { Popover, PopoverTrigger, PopoverContent } from "@uprise/ui";
