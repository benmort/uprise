/**
 * Inert marker for the first-nations kind — everything visible is rendered by the
 * persistent `(geo)` layout (chrome + GeoSurface). This file only exists to keep a
 * bookmarkable, prefetchable `/data/first-nations` segment + the kind contract.
 */
export default function FirstNationsPage() {
  return null;
}
