/**
 * Inert marker for the polling-places kind — everything visible is rendered by the
 * persistent `(geo)` layout (chrome + GeoSurface). This file only exists to keep a
 * bookmarkable, prefetchable `/data/polling-places` segment + the kind contract.
 */
export default function PollingPlacesPage() {
  return null;
}
