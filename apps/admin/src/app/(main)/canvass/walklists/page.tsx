// Campaign-less "All campaigns" walk lists. Same component as the scoped route — with no
// [campaignId] param, useParams() yields undefined and it lists turfs across every campaign.
export { default } from "../[campaignId]/walklists/page";
