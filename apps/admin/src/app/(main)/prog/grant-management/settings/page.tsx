import type { Metadata } from "next";
import { ComingSoon } from "@/components/prog/shared/coming-soon";

export const metadata: Metadata = {
  title: "Grant Settings | Admin",
  description: "Grant management settings",
};

export default function GrantManagementSettingsPage() {
  return <ComingSoon title="Grant Settings" description="Grant management settings" />;
}
