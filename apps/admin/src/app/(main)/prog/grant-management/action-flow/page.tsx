import type { Metadata } from "next";
import { ComingSoon } from "@/components/prog/shared/coming-soon";

export const metadata: Metadata = {
  title: "Action Flow | Admin",
  description: "Configure the grant action flow",
};

export default function GrantManagementActionFlowPage() {
  return <ComingSoon title="Action Flow" description="Configure the grant action flow" />;
}
