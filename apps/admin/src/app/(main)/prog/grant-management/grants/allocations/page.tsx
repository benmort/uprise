import type { Metadata } from "next";
import { ComingSoon } from "@/components/prog/shared/coming-soon";

export const metadata: Metadata = {
  title: "Grants – Allocations | Admin",
  description: "Manage grant allocations",
};

export default function GrantManagementGrantsAllocationsPage() {
  return <ComingSoon title="Grants – Allocations" description="Manage grant allocations" />;
}
