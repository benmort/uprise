import type { Metadata } from "next";
import { ComingSoon } from "@/components/prog/shared/coming-soon";

export const metadata: Metadata = {
  title: "Grants – Settings | Admin",
  description: "Grant settings",
};

export default function GrantManagementGrantsSettingsPage() {
  return <ComingSoon title="Grants – Settings" description="Grant settings" />;
}
