import type { Metadata } from "next";
import { ComingSoon } from "@/components/prog/shared/coming-soon";

export const metadata: Metadata = {
  title: "Grants – Payments | Admin",
  description: "Manage grant payments",
};

export default function GrantManagementGrantsPaymentsPage() {
  return <ComingSoon title="Grants – Payments" description="Manage grant payments" />;
}
