import type { Metadata } from "next";
import { ComingSoon } from "@/components/prog/shared/coming-soon";

export const metadata: Metadata = {
  title: "Grants – Users | Admin",
  description: "Manage grant users",
};

export default function GrantManagementGrantsUsersPage() {
  return <ComingSoon title="Grants – Users" description="Manage grant users" />;
}
