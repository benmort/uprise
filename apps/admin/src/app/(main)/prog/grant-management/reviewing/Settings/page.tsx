import type { Metadata } from "next";
import { ComingSoon } from "@/components/prog/shared/coming-soon";

export const metadata: Metadata = {
  title: "Reviewing – Settings | Admin",
  description: "Reviewing settings",
};

export default function GrantManagementReviewingSettingsPage() {
  return <ComingSoon title="Reviewing – Settings" description="Reviewing settings" />;
}
