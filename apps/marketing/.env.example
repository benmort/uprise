# API base for the marketing form endpoints (meld doc 12).
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
# Auth app — /sign-up and "Log in" link here (SSO hub).
NEXT_PUBLIC_AUTH_APP_URL=http://localhost:3002
