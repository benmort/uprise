> **Reconciliation note (post-audit, 2026-06-23).** This matrix was generated by an adversarial audit (wf_80302106) that ran concurrently with WS1/WS2. Two of its headline gaps are now CLOSED and any rows/items still marking them open are superseded:
> - **Tenant/user creation** — closed by WS1 (commit d558371): `apps/api/src/tenants/*` (create/update tenant, members, invitations, `/auth/register`, networks) + events.
> - **Empty reactions registry** — closed by WS2 (commit edd3807): `apps/api/src/common/reactions/domain-reactions.ts` wires welcome-email, invitation-email, network→Stripe-customer, subscription→tenant.
> The remaining open items below are the genuine WS3 backlog (segment CRUD + eval enqueue, 2FA enable/mobile-capture, checkout/portal endpoints, network read/billing-write, subdomain-availability, member lifecycle events) plus the deferred-by-design set (ES aggregates/replay, multi-org, OTT — no action).

# Parity Gap Backlog

## P0

- [ ] **Reaction registry is empty — every emitted event is a dangling producer** (tenant / identity / email / payment) — The REACTIONS provider is `useValue: []` (reactions.module.ts:13) with zero Reaction implementations. Events flow into the outbox and are catalogued (tenant.tenant.created, tenant.member.added, tenant.invitation.sent, tenant.network.created, tenant.org-credential.updated, iam.user.created, email.email.queued, payment.payment.succeeded/.refunded) but nothing consumes them: no welcome email, no invitation email, no Stripe-customer creation, no org-credential→payment sync. The producer side is wired; the consumer side of the entire reactions backbone is unbuilt. _Fix:_ Implement and register the cross-domain reactions — (1) tenant.invitation.sent → send invitation email (load token/role/expiry, render role + AEST expiry); (2) iam.user.created / tenant.tenant.created → welcome + provisioning flow; (3) tenant.network.created → create the Stripe customer; (4) tenant.org-credential.updated → payment/billing-profile sync. Add reaction-registry coverage asserting each catalogued event has at least one consumer.
- [ ] **Welcome email never fires on user creation** (email) — registration.service.ts:72 emits iam.user.created claiming it drives a welcome reaction, but REACTIONS is empty and no caller passes templateKey 'welcome'; the 'welcome' default template is dead. New users get no welcome email. Blocks WS1. _Fix:_ Register a Reaction on iam.user.created that calls EmailService.sendTransactional({templateKey:'welcome', toAddress, vars:{name,appName,signInUrl}, purpose:'welcome'}).
- [ ] **Invitation email is not sent** (email / tenant) — tenant.invitation.sent is emitted but no consumer exists, so invitees never receive a link, breaking the acceptInvite flow. The 'invitation' default body is just an accept link, omitting role + AEST-formatted expiry. _Fix:_ Register a Reaction on tenant.invitation.sent that loads the invitation (token, role, expiresAt) and calls sendTransactional({templateKey:'invitation', ...}); extend the template to render role + AEST expiry (port formatExpiresAtAEST).
- [ ] **No subdomain/slug availability pre-flight check** (tenant) — IsSubdomainAvailableHandler has no uprise equivalent; createTenant and register guard slug uniqueness reactively (ConflictException), so a signup UI cannot validate a desired subdomain before submit. _Fix:_ Add GET /tenants/subdomain-available?slug= → { slug, available, isAvailable } (trim/lowercase + prisma.tenant.findUnique). Public/pre-session via guard allowlist.
- [ ] **2FA is completely unreachable — no enable, no mobile capture, no mobile-verify** (identity) — login branches on user.twofaEnabled and start2fa requires user.mobile, but nothing ever sets twofaEnabled=true, writes User.mobile, or runs a verify-mobile flow. Enable2FA/Disable2FA/SendMobileVerification/VerifyMobilePhone are all absent; User.mobile/mobileVerified are dead columns; the ported start2fa/resend2fa/verify2fa is dead code. _Fix:_ Add addMobile + sendMobileVerification + confirmMobile (sets User.mobile + mobileVerified=true), enable2fa (gated on mobileVerified) and disable2fa, wired under /iam (POST /iam/mobile/send, /iam/mobile/confirm, /iam/2fa/enable, DELETE /iam/2fa).
- [ ] **No segment CRUD — users cannot create, edit, or delete dynamic segments** (audience) — The AudienceSegment model, a working evaluator, and AudiencesController exist, but there are ZERO segment write paths (grep finds only mocked prisma in specs); the controller exposes only Audience routes. The entire dynamic-segment feature is read-only. _Fix:_ Add a SegmentsService with create/update/delete over prisma.audienceSegment plus a controller, mirroring segment.handlers.ts (validate name, accept definition JSON + type + audienceId, tenant-scope); on create/rule-change enqueue SEGMENT_EVAL_RUN. Gate with the audience.segment permission; rely on AudienceSegmentMember onDelete:Cascade.
- [ ] **Nobody enqueues SEGMENT_EVAL_RUN — the wired evaluator never runs on real data** (audience) — The SegmentEvaluatorService is a faithful port with its consumer wired to the worker, but no producer ever adds a SEGMENT_EVAL_RUN job. AudienceSegmentMember is never materialised, so the blasts.service dynamic-segment recipient path always resolves to empty. _Fix:_ Enqueue SEGMENT_EVAL_RUN from the new SegmentsService on create and rule-changed update (getSegmentEvalJobId for idempotency); optionally enqueue when a contact's evaluator inputs change. Add an e2e: create-segment→worker materialises→blast resolves.
- [ ] **No HTTP/command surface for checkout & customer portal (adapter methods are dead code)** (payment) — StripeService.createCheckoutSession and createPortalSession are fully implemented against real Stripe REST but have ZERO callers — no controller route, no web UI. The core paid-conversion path is non-functional. _Fix:_ Add a BillingController with POST /billing/checkout-session and POST /billing/portal-session that resolve/ensure a Customer, call the adapters, and return the url. Wire the web billing page.
- [ ] **Payment method management entirely missing (add / set-default / remove)** (payment) — Prog has add/setDefault/remove handlers + attach/detach/setDefault adapters + a payment_method_view projection. Uprise has the PaymentMethod table and projectPaymentMethod but it is never called, and there are no attach/detach/setDefault adapters or routes. Customers cannot add, change or remove a card. _Fix:_ Add attachPaymentMethod/detachPaymentMethod/setDefaultPaymentMethod to StripeService, plus handlers/routes that drive projectPaymentMethod with exactly-one-default enforcement; mirror prog's first-method-becomes-default rule.

## P1

- [ ] **Network billing entity is inert; no GetNetwork and no billing-write onto Network** (tenant) — createNetwork exists but Network.planName/subscriptionStatus are never written, there is no GetNetwork read endpoint, and no GetTenantsByNetwork. The payment domain projects into a separate Subscription model, never onto Network, and payment.subscription.changed has no consumer. Network-level plan/status has nowhere to land and the session never carries it. _Fix:_ Add NetworkService.getNetwork + updateBilling({planName,subscriptionStatus}) and a reaction consuming payment.subscription.changed; decide canonical home for plan/status; surface it in membershipsFor/auth-check; add GetTenantsByNetwork.
- [ ] **Member and invitation lifecycle transitions emit no events (added is the only one)** (tenant) — removeMember, updateMemberRole, acceptInvite and revokeInvitation(decline) all mutate state without emitting an event (and the corresponding events are not catalogued). With the empty REACTIONS array, no reaction can observe a member leaving, a role change, an accept, or a decline. _Fix:_ Catalogue and emit tenant.member.removed, tenant.member.role-updated, tenant.invitation.accepted, tenant.invitation.declined inside the existing transactions; keep the inline write, make the fact observable.
- [ ] **No invitee-side decline-by-token; revoke is admin-only and silent on already_member** (tenant) — Decline exists only as admin revokeInvitation (manage tenant.invitation); there is no invitee-facing POST /iam/invite/decline {token}. addMember uses upsert so re-adding silently updates role instead of returning already_member, and removeMember has no last-owner guard. _Fix:_ Add POST /iam/invite/decline {token} (loadValidInvite + status 'declined' + emit tenant.invitation.declined); branch addMember on existing membership to throw ALREADY_MEMBER; add a last-owner guard to removeMember/updateMemberRole.
- [ ] **Tenant rename/delete and org-lifecycle invariants/events incomplete** (tenant) — updateTenant emits no TenantRenamed event and its DTO only exposes name; there is no soft-delete write path (deletedAt read-filtered in getTenant but never set; membershipsFor does NOT filter it); slug is only trim+lowercase (no SLUG_RE) and P2002 collisions surface raw in bypass paths; org-contact email-has-@ validation is absent. _Fix:_ Emit tenant.tenant.renamed; add a soft-delete command setting deletedAt + emit tenant.tenant.deleted and filter deletedAt in membershipsFor; port SLUG_RE and map P2002 to clean domain errors; add email validation to addContact/updateContact.
- [ ] **Identity is event-silent beyond registration; only 1 of 9 identity events fires and even that has no consumer** (identity) — Only iam.user.created is emitted (from register); acceptInvite does NOT emit it, and the other 8 prog identity events (UserSignedIn, EmailVerified, PasswordResetRequested, PasswordReset, ProfileUpdated, MobileVerified, TwoFactorEnabled/Disabled) are emitted nowhere. Cross-domain reactions to identity changes are impossible. _Fix:_ Emit iam.user.created from acceptInvite (create branch); add a welcome-email Reaction; as consumers land, emit the remaining identity events and extend DomainEventMap.
- [ ] **acceptInvite does not emit iam.user.created (second user-creation path is event-silent)** (identity) — register emits iam.user.created but acceptInvite creates a User in its own tx without appending any outbox event, so invited users never trigger downstream reactions. _Fix:_ Inject OutboxService into IamFlowsService and append iam.user.created inside the acceptInvite tx on the create branch only.
- [ ] **Sign-in events / lastSignInAt never recorded on any login path** (identity) — Prog records user.recordSignIn → UserSignedIn on every sign-in; uprise issues sessions silently on all paths (login, magic-link, 2fa, acceptInvite, register), writing no lastSignInAt and no event. Loses login audit/analytics. _Fix:_ Add a lastSignInAt column and update it in SessionService.create (single chokepoint); emit iam.user.signed-in once a consumer exists.
- [ ] **Sign-in password verification lives inline in the controller, not a service/handler** (identity) — iam.controller.ts:30-44 does lookup + verifyPassword + membership check + 2FA branch inline, making it untestable in isolation and not reusable. _Fix:_ Extract IamFlowsService.signInWithPassword(email,password) → {grant} | {twofaRequired, challengeId}; have IamController.login delegate.
- [ ] **UpdateProfile does not mirror displayName onto the base User row** (identity) — upsertProfile writes only UserProfile, so User.displayName drifts after a profile edit; anything reading User.displayName shows the stale name. _Fix:_ In upsertProfile, when input.displayName is defined, also update User.displayName inside the same transaction.
- [ ] **OTT issue/exchange capability missing** (identity) — Prog has IssueOtt/ExchangeOtt (5-minute single-use token for cross-app/admin session handoff); uprise has only the shared parent-domain cookie. _Fix:_ If a handoff flow is needed (admin impersonation, app-to-app deep link), add an Ott table + SessionService.issueOtt/consumeOtt; otherwise record as deliberately dropped. Default: defer until a concrete need surfaces.
- [ ] **recordSourceRecord never wired — ContactSourceRecord empty, hasSource clause dead** (audience) — ContactsService.recordSourceRecord is implemented and idempotent but called only from specs; the Action Network connector produces externalId yet integrations.service writes it onto AudienceContact, never to ContactSourceRecord. The hasSource segment clause matches nothing for imported contacts. _Fix:_ In the AN sync ingestion loop, call contacts.recordSourceRecord({tenantId, contactId, sourceSystem:'action_network', externalId, data}); add an integration test asserting a hasSource segment then resolves. (Confirm ContactSourceRecord @@unique [sourceSystem, externalId] is intended cross-tenant.)
- [ ] **No domain events emitted for person/segment/source-record changes (bus exists, audience unused)** (audience) — Every prog audience mutation publishes a namespaced event; uprise persists directly via Prisma with no emission, despite the outbox + reaction registry already being used by payment/tenants/calls, and DomainEventMap pre-declaring audience.imported and audience.segment.recomputed (both emitted nowhere). _Fix:_ Emit via OutboxService — wire audience.imported on import completion and audience.segment.recomputed at the end of evaluate; add audience.contact.upserted and segment.{created,updated,deleted} keys and emit them in the new SegmentsService and contact upsert paths.
- [ ] **ManageSourceRecords lacks remove + batch-reconcile** (audience) — recordSourceRecord only upserts a single record — no remove, no desired-set reconcile — so re-importing a shrinking source list leaves orphan rows that keep matching hasSource. _Fix:_ Add removeSourceRecord and reconcileSourceRecords (delete rows for (contact, sourceSystem) not in the desired externalId set) to ContactsService; call reconcile from each per-source sync.
- [ ] **No public segment read endpoints (get/list/members), including no canonical-person lookup** (audience) — Segments are read only internally (evaluator, blasts.service); the audience UI cannot list segments, view a definition, or preview membership. GetPerson's by-(canonicalPersonId, tenantId) read also has no equivalent since resolveIdentity never runs. _Fix:_ Add GET /audiences/:id/segments, GET /segments/:id, GET /segments/:id/members to the new SegmentsService/controller; once resolveIdentity is wired, expose GET /contacts/:id/canonical (or fold canonicalContactId into getProfile).
- [ ] **resolveIdentity never invoked — canonicalContactId never populated** (audience) — ContactsService.resolveIdentity implements the meld doc-10 canonical-identity grouping but no non-test code calls it, so canonicalContactId stays null and cross-source identity is never resolved. _Fix:_ Invoke resolveIdentity at ingestion seams where a new identifier appears (CSV import upsert, AN sync upsert, inbound-message contact creation), passing {email, phoneE164}.
- [ ] **No template management surface (CreateEmailTemplate / GetEmailTemplate missing)** (email) — resolveTemplate reads tenant overrides from EmailTemplate (with @@unique + isActive), but the only access is one findUnique; no create/update/upsert/get. Overrides can only exist via DB/seed, so the feature is unreachable. _Fix:_ Add EmailService.createTemplate (validate non-empty, upsert on @@unique) and getTemplate, plus a settings-scoped controller route mirroring prog's validation.
- [ ] **No email lifecycle events emitted (only queued, unconsumed)** (email) — Only email.email.queued is appended (one producer, no consumer); sent/sending/delivered/bounce/fail/open/click only mutate the row. No downstream domain can react to a bounce (suppress contact), a delivery, or engagement. _Fix:_ Append outbox events at each transition inside the existing prisma.email.update paths — at minimum email.email.bounced for list hygiene — consistent with the queued pattern.
- [ ] **No raw (non-template) send path; sendTemplate adapter feature unused; text/plain only** (email) — EmailService always resolves a templateKey and SendGridService.send posts text/plain only — no HTML body, no ad-hoc HTML send, no server-side dynamic-template path; prog templates produce HTML. _Fix:_ Add optional html/contentType to SendGridSendInput (emit text/html when body is HTML); add a direct-send overload (subject+body, no templateKey); optionally support a SendGrid dynamic-template id path.
- [ ] **GetEmail query missing (no read-back of an email by id)** (email) — The Email model persists status/openedAt/clickedAt/bounceReason/errorMessage/providerMessageId, but the only find calls are internal webhook correlation; no public getEmail and no route, so admin/debug and any delivery-status UI cannot query it. _Fix:_ Add EmailService.getEmail(tenantId,id) returning the tenant-scoped row plus a controller route. Cheap — fields already exist.
- [ ] **No proactive customer creation (createCustomer adapter + EnsureCustomer flow missing)** (payment) — Uprise only learns Customers from customer.created/updated webhooks; there is no StripeService.createCustomer and no EnsureCustomer handler, so checkout cannot attach a known customer. _Fix:_ Add StripeService.createCustomer({email, metadata}) and an ensureCustomer(tenantId/networkId, email) that returns or creates the Stripe customer + upserts the projection; call it from the new checkout handler.
- [ ] **No read queries for payments/refunds/invoices/subscriptions/payment-methods** (payment) — Prog exposes GetPayment, GetRefund, ListRefundsByPayment, GetPayments, GetInvoices, GetSubscriptions, GetPaymentMethods; uprise ports none. Invoice/Subscription rows are write-only via projections, Refund rows write-only. An admin billing page cannot list a tenant's billing data. _Fix:_ Add read methods (listPaymentsByTenant, getRefund, listRefundsByPayment, listInvoicesByTenant, listSubscriptionsByTenant, listPaymentMethodsByCustomer) on PaymentService/BillingService, exposed via BillingController behind tenant scoping. Straight Prisma finds.
- [ ] **recorded/processing/failed status changes emit no domain event** (payment) — Prog event-sources all five Payment transitions; uprise emits outbox events only for succeeded and refunded. recordPayment/markProcessing/markFailed mutate the row with no outbox.append, so dunning/failure-alerting reactions have nothing to subscribe to. _Fix:_ Emit payment.payment.recorded, .processing, and .failed (with failureReason) via outbox.append in the same tx as the write, matching the succeeded/refunded pattern. Low risk, additive.
- [ ] **No reaction consumes payment.succeeded / payment.refunded** (payment) — The events are emitted to the outbox but REACTIONS is empty; reaction-registry.spec asserts a dispatch is a no-op. If prog had cross-domain effects on payment success/refund (receipts, entitlement grants), those are not wired. _Fix:_ Confirm against prog whether success/refund triggered any reaction (receipt email, entitlement activation); if so, port it as a Reaction keyed on payment.payment.succeeded/.refunded; if not, document the events as produced for future consumers.
- [ ] **SMS Twilio status callback has no webhook-event idempotency claim** (telephony) — The voice callback claims (provider,eventId) before acting + releases on error, but the SMS path (blasts.handleTwilioStatusCallback) performs no claim and relies solely on column-state guards, so it lacks prog's idempotency layer-3 for SMS and a duplicate Twilio delivery can re-apply with failure-code/category churn. _Fix:_ Claim webhookEvents.claim('twilio-sms', `${messageSid}:${status}`) at the top of handleTwilioStatusCallback, return early if not claimed, release on error, mirroring CallsService.processStatusCallback.
- [ ] **Transactional SMS never advances past SENT — DELIVERED/UNDELIVERED FSM edges are dead** (telephony) — The status callback's OutboundMessage branch updates only the `status` column, never txStatus, so for TRANSACTIONAL rows the DELIVERED/UNDELIVERED transitions are unreachable and txStatus diverges from status; transactional messages are stuck at SENT. _Fix:_ When the OutboundMessage row is TRANSACTIONAL (txStatus != null), drive txStatus through assertValidTxSmsTransition (SENT→DELIVERED on delivered/read, →UNDELIVERED on undelivered, →FAILED on failed) instead of/alongside the status column.
- [ ] **No call lifecycle domain events emitted (only call.initiated)** (telephony) — Prog raises CallRinging/InProgress/Completed/Busy/NoAnswer/Failed; uprise's processStatusCallback writes the Call row directly with no outbox.append, so a canvassing/analytics reaction to a completed/failed call cannot subscribe. _Fix:_ Wrap the call.update in processStatusCallback in a $transaction with outbox.append of telephony.call.{ringing|in_progress|completed|busy|no_answer|failed} carrying callId, tenantId, and (for completed) duration/recording/price.
- [ ] **Blast (SmsCampaign) lifecycle emitted only as in-process realtime events, not durable outbox events** (telephony) — Prog emits SmsCampaignCreated/Scheduled/Sent; uprise emits RealtimeEventsService events only (blast.updated, blast.retry) for the UI, with zero outbox.append in blasts.service. Cross-domain reactions (analytics rollups, audience effects) cannot reliably subscribe. _Fix:_ Add outbox.append for messaging.blast.created (createDraft), .scheduled (schedule), and .sent (when status lands on SENT) inside the existing transactions, alongside the realtime emit.

## P2

- [ ] **GetUserTenants drops network context and soft-delete filtering** (tenant) — membershipsFor returns only {tenantId,tenantName,role}: no network plan/status join, no client-friendly aliases, no deletedAt filter. Low impact until reactions/networks are real. _Fix:_ Once Network billing + soft-delete are live, extend membershipsFor to join Network (planName/subscriptionStatus), filter deletedAt null, and add id/subdomain/displayName aliases if the admin client expects them.
- [ ] **Organisation collapsed to a single OrgProfile per tenant (no multi-org)** (tenant) — Prog models Organisation as a first-class aggregate with its own id space; uprise folds it into a 1:1 OrgProfile so GetOrganisationsByTenant degenerates to one row. _Fix:_ Accept as a deliberate simplification unless multi-org is a product requirement; if needed, promote OrgProfile to a tenant-scoped collection keyed by id with create/list-by-tenant endpoints.
- [ ] **Owner role modelled as ORGANISER, not a distinct OWNER** (tenant) — createTenant/register bootstrap the founding member as ORGANISER; prog distinguishes an owner, and there is no last-owner protection, so the founding member can be demoted or removed, orphaning a tenant. _Fix:_ Either add an OWNER role and bootstrap with it, or keep ORGANISER but add a last-organiser/last-owner guard so a tenant always retains at least one admin-capable member.
- [ ] **VerifyAccountRecoveryToken (pre-submit link validity check) missing** (identity) — Only POST /auth/reset-password (consuming) exists; no non-consuming validity check, so the reset UI cannot show 'link expired' before submit. _Fix:_ Add GET /auth/reset-password/:token/check returning {valid} (non-consuming findUnique + expiry/consumed check). UX-only, low effort.
- [ ] **ClearSelectedAvatar standalone op missing** (identity) — No standalone clear-selected op; only implicit de-selection inside deleteAvatar. Low value. _Fix:_ Optional: add ProfileService.clearSelectedAvatar(userId) + route only if a UI 'no avatar' affordance is needed; otherwise drop by design.
- [ ] **/auth/check omits mobileVerified and twofaEnabled** (identity) — /auth/check returns id/role/tenantId/memberships only, so a settings UI showing 2FA/mobile status needs an extra call. _Fix:_ Add mobileVerified and twofaEnabled to the /auth/check payload once those flows are wired (trivial once the User fields are populated).
- [ ] **No source-record list/read path** (audience) — Prog's ListSourceRecords exposes a person's external-system linkages; uprise has no read over ContactSourceRecord and getProfile omits provenance. Lower priority because the table is currently unpopulated. _Fix:_ Once recordSourceRecord is wired, add sourceRecords to getProfile (group ContactSourceRecord by sourceSystem) so the contact spine shows where a person came from.
- [ ] **delivered is terminal in uprise — late hard-bounce after delivery is dropped** (email) — EMAIL_TRANSITIONS makes DELIVERED terminal; prog allows delivered→bounced for late hard bounces, so a bounce webhook after delivered is a silent no-op. Low-frequency edge. _Fix:_ If late hard-bounces matter for suppression accuracy, add BOUNCED to the DELIVERED transition list and update the spec; otherwise document the divergence as intentional.
- [ ] **Webhook open/click use server time, not the SendGrid event timestamp** (email) — openedAt/clickedAt are set to new Date() ignoring event.timestamp, so under webhook retry/delay engagement times skew to processing time. _Fix:_ Use event.timestamp when present: openedAt = event.timestamp ? new Date(Number(event.timestamp)*1000) : new Date(); same for click.
- [ ] **No payment health probe** (payment) — Prog's HealthCheckHandler backs an admin Stripe-integration status alert; uprise has no payment health route (isConfigured() exists but isn't surfaced). _Fix:_ Add GET /billing/health returning {status, configured: stripe.isConfigured()}; optionally ping Stripe (GET /v1/balance) when configured to downgrade to degraded.
- [ ] **'undelivered' collapsed into FAILED — distinction lost** (telephony) — Both Twilio 'undelivered' (carrier rejected) and 'failed' (send error) map to BlastRecipientStatus.FAILED, which has no UNDELIVERED member, so the two are indistinguishable at the status level. _Fix:_ Either add an UNDELIVERED member to BlastRecipientStatus and branch on it, or ensure failureCategory/code from classifyFailureScope reliably separates carrier-undelivered (Twilio 30xxx) from internal failures for reporting.
- [ ] **No per-SMS read boundary (GetSms / GetSmsStatus)** (telephony) — Prog exposes GetSms/GetSmsStatus keyed by smsId; uprise has no get-message-by-id local read (no GET /messages/:id), so for a transactional 2FA send there's no API to query delivery. _Fix:_ Add GET /messages/:id returning OutboundMessage status/txStatus/errorMessage, tenant-scoped, permission-gated.
- [ ] **MessageTemplate has no CRUD, no variable validation, no type (sms|call_script)** (telephony) — Prog's MessageTemplate aggregate validates declared {{vars}}, supports type/category/variables[]/fromNumber and updateBody; uprise's model is id/tenantId/key/channel/kind/body/isActive only, read internally by resolveBody, with no create/update/get and no undeclared-variable guard. _Fix:_ Add a MessageTemplatesController + service for create/update/get (tenant-scoped, permission-gated), port the {{variable}}-must-be-declared validation, add type/variables if call-script templating is wanted; defer call_script if voice scripting is out of scope.
- [ ] **queued/sent set inline, not callback-driven; segmentCount not captured** (telephony) — Uprise sets QUEUED/SENT inline at dispatch and the SMS callback never reads NumSegments. _Fix:_ Optionally capture NumSegments on the status callback into BlastRecipient/OutboundMessage metadata; inline queued/sent is acceptable (matches Twilio acceptance semantics) and need not be reworked.
- [ ] **Event-sourcing aggregates / replay deliberately not ported** (audience / payment / telephony — deferred by design) — Person, Segment, Refund, Customer and the telephony/email aggregates are event-sourced in prog (raise/apply/fromEvents/replay); uprise models them as mutable Prisma rows with FSM guards + a transactional outbox per the meld plan. NA_HYBRID by design. _Fix:_ No port required. Confirm in the meld docs that dropped invariants (audience unique-email, refuse-update-when-deleted, async refund states) are intentional and document the divergences so they are not later mistaken for bugs; any future replay/audit need is met by the outbox stream once lifecycle events land.

---

## WS3 status (2026-06-23)

**Closed (committed on feat/prog-parity):**
- Tenant + user creation, member ops, invitations, networks, self-registration — WS1 (d558371).
- All cross-domain reactions (welcome/invitation/network→Stripe-customer/subscription→tenant) — WS2 (edd3807).
- Subdomain-availability pre-check (`GET /tenants/availability`) + member lifecycle events
  (`tenant.member.removed` / `tenant.member.role-updated`) — WS3.
- Latent fix: `createTenant` no longer FK-fails when the actor is the env super-admin (non-User).

**Genuinely remaining P1 (tracked, not yet built):**
- **Segment CRUD + eval enqueue** — `SegmentEvaluatorService` + the `segment-eval` worker exist but
  nothing creates an `AudienceSegment` or enqueues an evaluation; dynamic segments are unreachable
  end-to-end. _Fix:_ audience segment endpoints (create/update/list) + enqueue `SEGMENT_EVAL_RUN`.
- **2FA enable + mobile capture** — the SMS-2FA login flow (doc 14) is dead code: no endpoint sets
  `User.mobile` or flips `twofaEnabled`. _Fix:_ profile endpoints to capture+verify mobile then enable 2FA.
- **Stripe checkout/portal endpoints** — `StripeService.createCheckoutSession/createPortalSession`
  exist but have no controller (paid conversion unreachable). _Fix:_ `PaymentController` POST
  /payment/checkout-session + /payment/portal-session, gated `payment.all`.
- **Network reads/billing** — `GetNetwork`, `GetTenantsByNetwork`, and writing
  `Network.planName/subscriptionStatus` (currently inert).
- **DeleteTenant** soft-delete write + `membershipsFor` deletedAt filter.

**Deferred-by-design (no action):** event-sourced aggregates/replay, multi-org modelling, OTT handoff.

---

## WS3 final status (2026-06-23)

**Closed across the WS3 commits (feat/prog-parity):**
- Payment: checkout/portal/health endpoints + read queries (payments/refunds/invoices/subscriptions/payment-methods) + payment.status.changed event; owner role gains payment.all.
- Identity/tenant: 2FA enrolment + mobile capture; sign-in lastSignInAt; acceptInvite events (iam.user.created/member.added/invitation.accepted); displayName mirror; /auth/check flags; tenant soft-delete + membershipsFor deletedAt filter + last-organiser guard; network reads + billing-write (GetNetwork/GetTenantsByNetwork/UpdateNetworkBilling); subdomain-availability; member lifecycle events.
- Email: per-tenant template CRUD + getEmail; delivered/bounced events; late hard-bounce (delivered→bounced); webhook open/click use the SendGrid event timestamp; VerifyAccountRecoveryToken.
- Telephony: telephony.call.completed event.

**Remaining (tracked; deferred this pass):**
- **Audience sync wiring** — call ContactsService.recordSourceRecord + resolveIdentity from the Action Network import path (needs Contact-spine resolution inside the batched sync; higher regression surface) + audience domain events + source-record list/remove/reconcile. (Segment CRUD intentionally skipped per the brief.)
- **Telephony** — SMS Twilio-callback (provider,eventId) idempotency claim; transactional-SMS txStatus advance to DELIVERED/UNDELIVERED; per-SMS read (GET /messages/:id); MessageTemplate CRUD + type/category/variables/fromNumber + undeclared-variable guard; segmentCount capture; 'undelivered' BlastRecipientStatus member (enum migration).
- **Email** — ad-hoc HTML/dynamic-template send path (SendGridService currently text/plain only).
- **Identity** — OTT issue/exchange (effectively superseded by the doc-14 parent-domain cookie); ClearSelectedAvatar standalone op.

**Deferred-by-design (no action):** event-sourced aggregates/replay; multi-org Organisation aggregate; owner-as-distinct-OWNER enum role.
